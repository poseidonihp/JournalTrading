# Raíz del monorepo. Cada app tiene su propio .env.example en su carpeta.
# Ver apps/backend/.env.example para configuración del backend.
# Ver apps/frontend/.env.example si se necesita configuración en el frontend.
